<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/head', {title: entryName}); %>
</head>

<body>
  <div class="row">
    <div class="col s3 center"></div>
    <div class="col s6 center">
      <%- include('../partials/header'); %>

      <div class="section">
        <h4 class="center-align"><%= entryName %></h4>
      </div>
      <%- include('../partials/links', { entryData }); %>
      <%- include('../partials/tabs', { entryData }); %>

      <%- include('../partials/footer'); %>
    </div>
    <div class="col s3 center"></div>
  </div>
</body>

</html>
