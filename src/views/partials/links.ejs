<% if(entryData.overviewLinks) { %>

<div class="collection with-header">
  <div href="#!" class="collection-header center">
    <h5>Overview</h5>
  </div>

  <% for(const item of entryData.overviewLinks) { %>
  <a href="<%= item.url %>" class="collection-item"><%= item.name %></a>
  <a href="<%= item.url %>" class="collection-item"><%= item.name %></a>
  <% } %>

</div>

<% } %>
