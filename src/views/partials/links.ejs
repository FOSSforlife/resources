<% for(const [sectionTitle, section] of Object.entries(sections)) { %>
<% if(Array.isArray(section)) { %>
<%- include('./links-section', {sectionTitle, items: section}) %>
<% } else {%>
<%- include('./links-section-subsections', {sectionTitle: sectionTitle, subSections: section}) %>
<% }%>
<% }%>
